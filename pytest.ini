[pytest]
# Make the project root importable so tests can `import app...` (namespace pkgs).
pythonpath = .
testpaths = tests
# Custom marker for the LLM-as-judge evals (require model keys; run explicitly).
markers =
    judge: LLM-as-judge evaluation tests (need model keys + a dataset)
